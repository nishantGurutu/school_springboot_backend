spring.application.name=school_app

# JWT secret - keep this in sync across instances; override via env JWT_SECRET for production
app.jwt.secret=${JWT_SECRET:super-secret-key-super-secret-key-super-secret-key}
# Database Connection
# 1. Local PostgreSQL (Use this if you are using pgAdmin installed on your local computer):
# spring.datasource.url=jdbc:postgresql://localhost:5432/school_db
# spring.datasource.username=postgres
# spring.datasource.password=postgres

# 2. Remote Render Cloud PostgreSQL DB:
#spring.datasource.url=jdbc:postgresql://dpg-dahdl767bikc73fg3hd0-a.singapore-postgres.render.com:5432/school_db_aeks?sslmode=require&sslfactory=org.postgresql.ssl.NonValidatingFactory
spring.datasource.url=jdbc:postgresql://localhost:5432/test_db
spring.datasource.username=postgres
spring.datasource.password=123456
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update
logging.level.org.springframework.data.jpa.repository.query=DEBUG
logging.level.org.hibernate.SQL=DEBUG
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jackson.time-zone=Asia/Kolkata
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Kolkata
server.port=${PORT:8080}
management.endpoints.web.exposure.include=health
management.endpoint.health.show-details=always

# Swagger UI: http://localhost:8080/

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.operationsSorter=method

# CORS - comma separated origins allowed to call backend APIs (development)
app.cors.allowed-origins=http://localhost:3000

# File upload
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=55MB
app.upload.dir=uploads

# Face verification microservice (FastAPI)
app.face-verify.base-url=http://127.0.0.1:8000
app.face-verify.enabled=true
